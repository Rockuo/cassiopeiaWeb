/**
 * Created by rockuo on 4.6.17.
 */


export const page = (state = '') => state;
export const user = (state = '') => state;
export const menuItems = (state = []) => state;
export const topMenuItems = (state = []) => state;
export const pageData = (state = {}) => state;